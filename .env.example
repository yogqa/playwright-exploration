# ── Antigravity Framework Environment Variables ──
# Copy this file to .env and fill in your values.

# Base URL of the application under test
BASE_URL=https://automationexercise.com

# Test user credentials (must be a pre-registered account on automationexercise.com)
# Run TC1 (Register User) once to create this account, then set these values.
USER_EMAIL=testuser@antigravity.dev
USER_PASSWORD=Test@1234

# Full name used during signup flows
USER_NAME=Antigravity Tester

# API token for apiContext fixture (optional — only needed if the API requires auth)
# API_TOKEN=your_token_here

# CI flag (set automatically by CI runners)
# CI=true
